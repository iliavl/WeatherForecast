//
//  Test.swift
//  WeatherForecast
//
//  Created by LIV on 06.07.17.
//  Copyright © 2017 LIV. All rights reserved.
//

import UIKit

class Test: NSObject {

//    let documentsDirectory = FileManager.default.urls(for: .documentDirectory, in: .userDomainMask).first!
//    let imagePath = documentsDirectory.appendingPathComponent("CarImages/\(fileName)")
//    guard let url = URL(string: urlStr) else { return }
//        URLSession.shared.dataTask(with: url) { (data, response, error) in
//    guard
//    let httpURLResponse = response as? HTTPURLResponse, httpURLResponse.statusCode == 200
//    let mimeType = response?.mimeType, mimeType.hasPrefix("image")
//    let data = data, error == nil, let image = UIImage(data: data)
//    else { return }
//    let pngImage = UIImagePNGRepresentation(image)
//    do { try pngImage?.write(to: imagePath, options: .atomic)
//    }
//    catch let error as NSError {
//    print("Error saving image: \(error.localizedDescription)")
//    }
//    }.resume()
//    
//    let documentsURL = FileManager.default.urls(for: .documentDirectory, in: .userDomainMask).first!
//    let filePath = documentsURL.appendingPathComponent("CarImages/\(fileName)")
//    if FileManager.default.fileExists(atPath: filePath.path) {
//    image = UIImage(contentsOfFile: filePath.path)
//    }
    
}
